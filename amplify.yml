version: 1
frontend:
  phases:
    preBuild:
      commands:
        # キャッシュを活用してビルド時間を短縮
        - npm ci --prefer-offline --no-audit
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*

